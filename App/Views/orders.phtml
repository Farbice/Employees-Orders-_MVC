<h1>Liste des commandes</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Numéro</th>
            <th>Date de commande</th>
            <th>Statut</th>
            <th>Nom du client</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($orders as $order): ?>
            <tr>
                <td><a href="?page=order-detail&id=<?= $order['orderNumber'] ?>"><?= $order['orderNumber'] ?></a></td>
                <td><?= $order['orderDate'] ?></td>
                <td><?= $order['status'] ?></td>
                <td><?= $order['customerName'] ?></td>
            </tr>
        <?php endforeach ?>
    </tbody>
</table>